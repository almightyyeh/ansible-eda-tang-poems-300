---
- name: Listen for event on a webhook
  hosts: all
  sources:
      - ansible.eda.webhook:
            host: 0.0.0.0
            port: 5000
  rules:
      - name: negation
        condition: not (event.i > 50 or event.i < 10)
        action:
            print_event:
